const arry = new Array(1, 2, 3, 4, 5, 6);   //配列のインデックスは 0 から始まる
// console.log(arry[3]);

console.log(arry[3]);
// 一度、配列のインデックス3を表示させる
arry[7] = 10;
// 配列の

const arry = 10;
const arry = new Array(1, 2);

console.log(arry);

// const arry = [1, 2, 3, 4, 5, 6];    //一般的にはこの記述で配列を作成する

// const arry = [1, 2, 3, 4, 5, 6, 'moji', false];    //配列内には様々なデータ型を格納できる(JavaScriptの特徴)
// console.log(arry.length);   //配列の要素数を取得

// arry.push('new item');   // 配列の最後に追加される
// arry.unshift('new item');   // 配列の先頭に追加される
// arry.pop();                 // 配列の最後の要素が削除される
// const val = arry.pop();     // 最後尾の削除される要素が変数へ代入される
// const val = arry.shift();   // 先頭の削除される要素が変数へ代入される
// console.log(arry);

// JavaScript では変数名で大文字と小文字は区別されるが、プロジェクト内で混在させるとバグの原因になるため小文字で統一するのが良い
